source:
  file: topics.json
destination:
  bootstrap_servers: 127.0.0.1:9092
  update_existing_topics: false
  max_version: "3.0.0" # maximum kafka protocol version
  tls:
    enabled: false
  replication_factor: 1 # If defined, override the replication factor